# Homework

Each assignment belongs in its own folder:

- `homework0/` - Stage 00 environment and Jupyter check.
- `homework1/`, `homework2/`, and later folders - create them only when the matching assignment begins.

Copy starter files from `class_materials/` before editing. Commit the homework copy, not the instructor original.
